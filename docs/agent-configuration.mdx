---
id: agent-configuration-overview
title: Pyroscope Agent Configuration
sidebar_label: Overview
slug: /agent-configuration-overview
description: Pyroscope Agent configuration
keywords: [ pyroscope, agent, config, configure, configuration, setup, options, arguments, cli, env, environment, variables ]
---

There are 3 ways to configure **Pyroscope Agent**. Configuration precedence is evaluated in the following order:
* command line arguments
* environment variables
* configuration files

Configuration options are different depending on how **Pyroscope Agent** is running:
* [`pyroscope exec`](/docs/agent-configuration-exec)
* [`pyroscope connect`](/docs/agent-configuration-connect)
* [`pyroscope agent`](/docs/agent-configuration-agent)
* [`pyroscope adhoc`](/docs/agent-configuration-adhoc)

## Configuration files

Configuration files are stored in [YAML format](https://yaml.org/start.html). You can specify configuration file
location with `-config <path>` argument, for example:

```shell
pyroscope agent -config /tmp/my-config.yml {args}
```

Or an environment variable, for example:

```
PYROSCOPE_CONFIG=/tmp/my-config.yml pyroscope exec {args}
```

## Environment variables

Environment variables **MUST** have `PYROSCOPE_` prefix and be in `UPPER_SNAKE_CASE` format, for example:
```shell
PYROSCOPE_API_BIND_ADDR=:9999 pyroscope server
```

---
id: agent-install-linux
title: Linux
sidebar_label: Linux
slug: /agent-install-linux
description: Install Pyroscope Agent on Ubuntu, Debian, CentOS, RHEL, Fedora, Amazon Linux
keywords: [ pyroscope, agent, install, deploy, deployment, linux, rpm, deb, package, ubuntu, debian, centos, rhel, fedora, amazon ]
---


import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import CodeBlock from '@theme/CodeBlock';
import packages from '../packages.manifest.json';
import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';
import { faLinux } from '@fortawesome/free-brands-svg-icons';


This guide will help you install **Pyroscope Agent** on <FontAwesomeIcon color="#0db7ed" icon={faLinux}/> Linux.

## Install

Pyroscope team maintain and sign packages for the following Linux distributions:

<Tabs
  defaultValue="ubuntu"
  values={[
    {label: 'Ubuntu/Debian', value: 'ubuntu'},
    {label: 'CentOS/RHEL', value: 'centos'},
    {label: 'Fedora', value: 'fedora'},
    {label: 'Amazon Linux', value: 'amazon-linux'},
  ]}>

  <TabItem value="ubuntu">
    Download package:
    <CodeBlock className="language-shell">
      {
        `wget https://dl.pyroscope.io/release/pyroscope_${Object.keys(packages.releases)[0].substr(1)}_amd64.deb`.replace(/^\s+/mg, "")
      }
    </CodeBlock>
    <br/>Install with apt-get:
    <CodeBlock className="language-shell">
      {
        `sudo apt-get install ./pyroscope_${Object.keys(packages.releases)[0].substr(1)}_amd64.deb`.replace(/^\s+/mg, "")
      }
    </CodeBlock>
  </TabItem>

  <TabItem value="centos">
    Download package:
    <CodeBlock className="language-shell">
      {
        `wget https://dl.pyroscope.io/release/pyroscope-${Object.keys(packages.releases)[0].substr(1)}-1-x86_64.rpm`.replace(/^\s+/mg, "")
      }
    </CodeBlock>
    <br/>Install with yum:
    <CodeBlock className="language-shell">
      {
        `sudo yum localinstall pyroscope-${Object.keys(packages.releases)[0].substr(1)}-1-x86_64.rpm`.replace(/^\s+/mg, "")
      }
    </CodeBlock>
  </TabItem>

  <TabItem value="fedora">
    Download package:
    <CodeBlock className="language-shell">
      {
        `wget https://dl.pyroscope.io/release/pyroscope-${Object.keys(packages.releases)[0].substr(1)}-1-x86_64.rpm`.replace(/^\s+/mg, "")
      }
    </CodeBlock>
    <br/>Install with dnf:
    <CodeBlock className="language-shell">
      {
        `sudo dnf install pyroscope-${Object.keys(packages.releases)[0].substr(1)}-1-x86_64.rpm`.replace(/^\s+/mg, "")
      }
    </CodeBlock>
  </TabItem>

  <TabItem value="amazon-linux">
    Download package:
    <CodeBlock className="language-shell">
      {
        `wget https://dl.pyroscope.io/release/pyroscope-${Object.keys(packages.releases)[0].substr(1)}-1-x86_64.rpm`.replace(/^\s+/mg, "")
      }
    </CodeBlock>
    <br/>Install with yum:
    <CodeBlock className="language-shell">
      {
        `sudo yum localinstall pyroscope-${Object.keys(packages.releases)[0].substr(1)}-1-x86_64.rpm`.replace(/^\s+/mg, "")
      }
    </CodeBlock>
  </TabItem>

</Tabs>

:::tip
To find out about other ways of installing Pyroscope, or to find packages for other CPU architectures (e.g ARM) visit our [Downloads](/downloads) page.
:::

**Pyroscope Agent** is not installed as `systemd` service. If you need to profile a process which is running as a service,
you may wrap `ExecStart` option of the `systemd` unit with `pyroscope exec`.

## Verify the installation

To verify **Pyroscope Agent** was installed correctly, try the `pyroscope` command:
```
pyroscope
```

You should see output, similar to the following:
```
 _ __  _   _ _ __ ___  ___  ___ ___  _ __   ___
| '_ \| | | | '__/ _ \/ __|/ __/ _ \| '_ \ / _ \
| |_) | |_| | | | (_) \__ \ (_| (_) | |_) |  __/
| .__/ \__, |_|  \___/|___/\___\___/| .__/ \___|
| |     __/ |                       | |
|_|    |___/                        |_|

continuous profiling platform

USAGE
  pyroscope [flags] <subcommand>

SUBCOMMANDS
  server   starts pyroscope server. This is the database + web-based user interface
  exec     starts a new process from <args> and profiles it
  connect  connects to an existing process and profiles it

FLAGS        DEFAULT VALUES

  -version  false

Run 'pyroscope SUBCOMMAND --help' for more information on a subcommand.
```

## Next steps

Refer to [Configuration](/docs/agent-configuration-overview) and [Integrations](/docs/supported-integrations) pages to learn more about
**Pyroscop Agent** and how to configure it.
